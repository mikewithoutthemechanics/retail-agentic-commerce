# Placeholder secret manifest.
#
# In production these values MUST NOT be committed. Use the External Secrets
# Operator (or Sealed Secrets) to project them from your cloud secret manager:
#
#   aws-secret-manager, gcp-secret-manager, azure-keyvault, or vault.
#
# Example ExternalSecret (do not commit the raw values):
#   apiVersion: external-secrets.io/v1beta1
#   kind: ExternalSecret
#   metadata: { name: acp-secrets, namespace: acp }
#   spec:
#     data:
#       - secretKey: DATABASE_URL
#         remoteRef: { key: acp/prod, property: database_url }
#       - secretKey: MERCHANT_API_KEY
#         remoteRef: { key: acp/prod, property: merchant_api_key }
#       - secretKey: PSP_API_KEY
#         remoteRef: { key: acp/prod, property: psp_api_key }
#       - secretKey: WEBHOOK_SECRET
#         remoteRef: { key: acp/prod, property: webhook_secret }
#       - secretKey: NVIDIA_API_KEY
#         remoteRef: { key: acp/prod, property: nvidia_api_key }
apiVersion: v1
kind: Secret
metadata:
  name: acp-secrets
  namespace: acp
  labels:
    app.kubernetes.io/part-of: agentic-commerce
type: Opaque
stringData:
  DATABASE_URL: "REPLACE_ME"
  MERCHANT_API_KEY: "REPLACE_ME"
  PSP_API_KEY: "REPLACE_ME"
  WEBHOOK_SECRET: "REPLACE_ME"
  NVIDIA_API_KEY: "REPLACE_ME"
